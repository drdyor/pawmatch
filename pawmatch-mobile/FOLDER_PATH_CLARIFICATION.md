# 📁 Folder Path Clarification

## The Answer:

**GitHub Repository Path:** `pawmatch-mobile/` (subfolder)

**Local Path:** `/workspace/pawmatch-mobile/`

**In Git:** Files are tracked as `pawmatch-mobile/src/...`, `pawmatch-mobile/App.tsx`, etc.

---

## 🎯 For Expo Snack Import:

When Snack asks for "Project Path" or "Directory":
- **Enter:** `pawmatch-mobile`

OR use the full path:
- `pawmatch-mobile/` 

---

## 📂 Full Structure:

```
GitHub Repo Root: drdyor/pawmatch
  └── pawmatch-mobile/          ← This is the folder path
      ├── App.tsx
      ├── package.json
      ├── src/
      │   ├── components/
      │   ├── screens/
      │   ├── navigation/
      │   └── ...
      └── ...
```

---

## ✅ Summary:

**Folder Path = `pawmatch-mobile`**

That's it! Just `pawmatch-mobile` (no leading slash, no trailing slash needed in most tools).
